# Environment configuration for local development
# Copy to `.env.local` and fill with real values as needed

# NextAuth
NEXTAUTH_SECRET=change-me-in-dev
NEXTAUTH_URL=http://localhost:3000

# Database (placeholder until DB is added)
DATABASE_URL=postgres://user:password@localhost:5432/insightify

# Public app metadata
NEXT_PUBLIC_APP_NAME=InSightify – Hospitality
